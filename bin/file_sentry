#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../../lib', __FILE__))

require 'file_sentry'
require 'file_sentry/command_line'

op = FileSentry::CommandLine.new(filepath: ARGV[0], encryption: ARGV[1])
op.start_utility
