#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../../lib', __FILE__))

require 'file_sentry'
require 'file_sentry/command_line'

FileSentry::CommandLine.new(ARGV).start_utility
